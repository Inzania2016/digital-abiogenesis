# R1C Canonical Bacterium-0 Benchmark Evidence Verification

Date: 2026-07-26  
Runtime: Python 3.13.13 in `.venv`  
Source commit: `7164e571810deac67042b87e3a5eacddfc24f6f0`

## Scope

This record covers preflight, execution, independent validation, explicit
artifact audits, runtime projection, frozen reporting, and control-plane
reconciliation for all five quick and all five full Bacterium-0 benchmark-v1
scenarios. It does not claim pygame visual verification.

## Execution Gate and Preflight

Commands:

```powershell
git status --short
git rev-parse HEAD
git rev-parse --abbrev-ref --symbolic-full-name '@{u}'
git status --short --branch
git rev-parse origin/main
git check-ignore -v runs runs/r1c runs/r1c/quick runs/r1c/full runs/r1c/r1c-execution-ledger.json
git ls-files runs
.\scripts\check.ps1
```

Results:

- `git status --short` produced no output.
- `HEAD` and `origin/main` were
  `7164e571810deac67042b87e3a5eacddfc24f6f0`.
- `main` tracked `origin/main` with no ahead/behind state.
- `.gitignore` rule `runs/` covered every R1C evidence path.
- `git ls-files runs` produced no output.
- the preflight quality gate passed: 126 tests, Ruff lint, and Ruff format
  check; pytest reported one upstream NEAT exporter deprecation warning.

No tracked file was edited before the ten canonical runs completed.

## Quick Execution and Validation

Commands, in approved order:

```powershell
.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario stable-default-v1 -Suite b0-quick-v1 -OutputRoot runs/r1c/quick
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/quick/20260726T204532Z_stable-default-v1_b0-quick-v1_9886

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario sparse-food-v1 -Suite b0-quick-v1 -OutputRoot runs/r1c/quick
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/quick/20260726T204621Z_sparse-food-v1_b0-quick-v1_1a3d

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario poison-rich-v1 -Suite b0-quick-v1 -OutputRoot runs/r1c/quick
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/quick/20260726T204704Z_poison-rich-v1_b0-quick-v1_8273

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario resource-shift-v1 -Suite b0-quick-v1 -OutputRoot runs/r1c/quick
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/quick/20260726T204730Z_resource-shift-v1_b0-quick-v1_6dd2

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario unseen-seeds-v1 -Suite b0-quick-v1 -OutputRoot runs/r1c/quick
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/quick/20260726T204757Z_unseen-seeds-v1_b0-quick-v1_940c
```

| Scenario | Run ID | Wall clock | Status | Validation |
| --- | --- | ---: | --- | --- |
| stable default | `20260726T204532Z_stable-default-v1_b0-quick-v1_9886` | 5.867 s | completed | passed |
| sparse food | `20260726T204621Z_sparse-food-v1_b0-quick-v1_1a3d` | 5.273 s | completed | passed |
| poison rich | `20260726T204704Z_poison-rich-v1_b0-quick-v1_8273` | 4.961 s | completed | passed |
| resource shift | `20260726T204730Z_resource-shift-v1_b0-quick-v1_6dd2` | 5.766 s | completed | passed |
| unseen seeds | `20260726T204757Z_unseen-seeds-v1_b0-quick-v1_940c` | 5.789 s | completed | passed |

Quick total: 27.656 seconds.

## Quick Artifact Audit

After each validate-only command, a PowerShell JSON audit loaded
`manifest.json` and `metrics.json` and required:

- contract `1.0`, benchmark `bacterium-0-v1`, and matching scenario/suite;
- clean source commit
  `7164e571810deac67042b87e3a5eacddfc24f6f0`;
- roots `[21, 22, 23]`, budgets 200/20, and exact canonical policy order;
- scenario-specific training/evaluation configuration;
- explicit `novelty_reward=0.02` only for the shaped policy;
- every policy and seed status `completed`;
- every seed `sample_count=20`;
- every universal metric non-null;
- every aggregate count equal to 3;
- zero deviations and no policy directory or policy declarations.

All checks passed. The validator independently checked hashes, arithmetic,
summary headings, paths, and declared inventory.

## Runtime Projection and Full-Suite Gate

Three timed dry runs measured fixed command overhead:

```powershell
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --dry-run --scenario stable-default-v1 --suite b0-quick-v1
```

Observed times were 0.493, 2.389, and 0.469 seconds; median overhead was 0.493
seconds.

Quick work was 2700 policy episodes and full work was 45000, a 16.666667
ratio. For each scenario:

```text
projected = 1.20 * (overhead + (quick wall clock - overhead) * work ratio)
```

| Scenario | Conservative projection |
| --- | ---: |
| stable default | 108.080 s |
| sparse food | 96.200 s |
| poison rich | 89.960 s |
| resource shift | 106.060 s |
| unseen seeds | 106.520 s |
| **Aggregate** | **506.820 s (8.447 min)** |

The 8-hour aggregate and 2-hour per-scenario gates both passed. All quick runs
were completed, validated, shared one clean source identity, had exact
configuration, contained no contradictions found by audit, and declared no
deviations. Full execution therefore proceeded.

## Full Execution and Validation

Commands, in approved order:

```powershell
.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario stable-default-v1 -Suite b0-full-v1 -OutputRoot runs/r1c/full
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/full/20260726T204940Z_stable-default-v1_b0-full-v1_ef82

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario sparse-food-v1 -Suite b0-full-v1 -OutputRoot runs/r1c/full
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/full/20260726T205148Z_sparse-food-v1_b0-full-v1_724b

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario poison-rich-v1 -Suite b0-full-v1 -OutputRoot runs/r1c/full
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/full/20260726T205344Z_poison-rich-v1_b0-full-v1_ba0c

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario resource-shift-v1 -Suite b0-full-v1 -OutputRoot runs/r1c/full
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/full/20260726T205534Z_resource-shift-v1_b0-full-v1_c859

.\scripts\run-benchmark.ps1 -Mode Benchmark -Scenario unseen-seeds-v1 -Suite b0-full-v1 -OutputRoot runs/r1c/full
.\.venv\Scripts\python.exe -m abiogenesis.benchmark.runner --validate-only runs/r1c/full/20260726T205743Z_unseen-seeds-v1_b0-full-v1_ea9c
```

| Scenario | Run ID | Wall clock | Status | Validation |
| --- | --- | ---: | --- | --- |
| stable default | `20260726T204940Z_stable-default-v1_b0-full-v1_ef82` | 103.361 s | completed | passed |
| sparse food | `20260726T205148Z_sparse-food-v1_b0-full-v1_724b` | 92.100 s | completed | passed |
| poison rich | `20260726T205344Z_poison-rich-v1_b0-full-v1_ba0c` | 86.589 s | completed | passed |
| resource shift | `20260726T205534Z_resource-shift-v1_b0-full-v1_c859` | 104.291 s | completed | passed |
| unseen seeds | `20260726T205743Z_unseen-seeds-v1_b0-full-v1_ea9c` | 103.484 s | completed | passed |

Full total: 489.825 seconds (8.164 minutes).

## Full Artifact Audit

The quick-audit predicates were repeated with roots 21-30, budgets 1000/100,
seed `sample_count=100`, and aggregate count 10. Resource shift additionally
required stable training `food_count=8` and sparse evaluation `food_count=4`.
All checks passed with zero deviations and no policy artifacts.

Machine-readable comparison also confirmed that stable-default and unseen-seed
policy payloads are identical in both quick and full evidence. This is recorded
as a benchmark-v1 limitation, not hidden.

## Artifact Retention

The ten immutable run directories and
`runs/r1c/r1c-execution-ledger.json` remain local and ignored. The commands did
not use `-KeepPolicies`; no run contains a `policy/` directory. Nothing under
`runs/` is tracked or staged.

## Frozen Report

Numerical interpretation is frozen in:

```text
docs/experiments/BACTERIUM_0_V1_BENCHMARK_REPORT.md
```

The report derives tables from `metrics.json`, separates shaped and unshaped
returns, retains mixed and negative findings, records adjacent root overlap,
and makes no significance or visual claim.

## Post-Report Quality Gate

```powershell
.\scripts\check.ps1
```

Result: passed with 126 tests, Ruff lint, and Ruff format check. Pytest reported
the same one upstream NEAT exporter deprecation warning.

## Verification Boundaries and Deviations

- No source, test, dependency, configuration, or organism behavior changed.
- No pygame command was run; no visual behavior is claimed.
- The adjacent replicate-root overlap was accepted and documented.
- Generated policies were intentionally not retained.
- No runner, validator, or artifact defect appeared.
- All canonical manifests declared zero deviations.
- No commit or push was performed.

## Final Git Inspection

```powershell
git diff --check
git status --short
git status --short --branch
git status --ignored --short runs
git ls-files runs
```

Results:

- `git diff --check` passed with no output.
- `main` still tracked `origin/main`.
- only R1C report, verification, chronology, navigation, decision, state, roadmap, and
  next-packet documents were modified or created.
- `git status --ignored --short runs` reported `!! runs/`.
- `git ls-files runs` produced no output.
- nothing was staged, committed, or pushed.
