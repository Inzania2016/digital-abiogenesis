# R0 Verification Record — 2026-07-20

## Scope and Environment

- Milestone: R0 Project Rebaseline and Engineering Retool.
- Workspace: repository root.
- Runtime: Python 3.13.13 in the preserved `.venv`.
- Key installed tools: pytest 9.0.3, Ruff 0.15.22, pygame 2.6.1.
- Git metadata: absent from the workspace and all inspected ancestors.

No pygame window was opened, observed, or visually verified by Codex.

## Command Record

### Pre-change test baseline

```powershell
.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider
```

Result: **failed**. Pytest collected 87 tests; 77 passed and 10 errored during `tmp_path`
fixture setup because the machine-level pytest temp root was permission-inaccessible. No
assertion failures were reported. R0 addressed this in the project test script by selecting
ignored repository-local temporary storage.

### Bootstrap

```powershell
.\scripts\bootstrap.ps1
```

Result: **passed**. The existing Python 3.13 virtual environment was preserved, packaging
tools were upgraded, and the editable `.[dev,render]` dependency set installed. Final
reported runtime: Python 3.13.13.

### Quality gate attempts

```powershell
.\scripts\check.ps1
```

First R0 attempt: tests passed, then Ruff identified one pre-existing unused local assignment
in `pygame_renderer.py`. The dead assignment was removed without changing behavior.

Second attempt: tests and lint passed, then the new formatter check identified 14 existing
Python files requiring mechanical Ruff formatting. `ruff format .` reformatted those files.

Final result: **passed**.

- 87 tests passed.
- Ruff lint passed.
- Ruff format check passed for 39 Python files.

### Benchmark smoke verification

```powershell
.\scripts\run-benchmark.ps1
```

Result: **passed** in approximately 10 seconds. It ran seeds 21-23, 200 training episodes per
Q agent, 20 evaluation episodes per seed, and a 10x10 grid. The output reproduced the existing
documented Phase 3D table, including:

| Metric | Random | Local Q | Raw scent | Conflict | Memory | Visit | Loop | Novelty |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Average reward | 0.227 | 1.704 | 0.378 | 0.564 | 0.192 | 0.213 | 0.319 | 0.137 |
| Poison collisions | 9.333 | 4.333 | 8.667 | 10.333 | 12.333 | 7.333 | 10.333 | 8.000 |
| Unique tiles | 219.667 | 271.667 | 183.667 | 186.000 | 196.000 | 193.333 | 199.333 | 191.667 |

This is a smoke comparison against preserved evidence, not a new learning claim.

### Cleanup

```powershell
.\scripts\clean.ps1
```

Result: **incomplete with a clear error**. It removed `.pytest-tmp`, `.ruff_cache`, all found
`__pycache__` directories, and editable `*.egg-info`. It preserved `.venv`, source assets,
and experiment records. The pre-existing root `.pytest_cache` could not be removed because
the path is permission-locked; the script reported that single failure and exited nonzero.

### Source archive

```powershell
.\scripts\package-source.ps1
```

Result: **passed**. It created `dist/digital-abiogenesis-source.zip`. Archive inspection found
76 entries and zero forbidden entries matching virtual environments, caches, bytecode,
build/dist content, models, runs, artifacts, screenshots, outputs, or
`.obsidian/workspace.json`. Shared Obsidian `app.json`, `appearance.json`, and
`core-plugins.json` were included. Initial inspected archive SHA-256:
`A747ACE9FE6046B84CA285DAE707C7849F72A54C8F11D085151EB43AF8788562`.

The archive is regenerated after final documentation updates, so the final file hash may
differ while the exclusion assertions remain the same.

### PowerShell syntax

All six files under `scripts/` parsed successfully with the PowerShell language parser.

### Git status

```powershell
git status --short
```

Result: **unavailable**. Git reported that the workspace is not a Git repository. No commit
or push was attempted.

## Manual Verification Boundary

The pygame visual procedure in `VERIFICATION.md` was skipped. Only Joe may mark sprite
appearance, animation, controls, HUD behavior, or general human-visible behavior as verified.

## Behavior-change Statement

No organism behavior was intentionally changed. Environment rules, rewards/defaults, agent
learning, encoder semantics, experiment results, metrics, and pygame features were preserved.
Python changes were limited to mechanical Ruff formatting and removal of one unused local
assignment. The canonical benchmark reproduced the historical values exactly.

## 2026-07-21 Clean-source Audit Addendum

The revival-plan audit identified one R0 exit criterion that had not previously been tested
from a genuinely clean source tree. The final filtered source archive was extracted to the
isolated `dist/r0-clean-audit` directory, which initially contained no `.venv`.

Commands executed from that extracted copy:

```powershell
.\scripts\bootstrap.ps1
.\scripts\check.ps1
```

Result: **passed**.

- `bootstrap.ps1` created a new Python 3.13.13 virtual environment.
- The editable `.[dev,render]` set installed successfully, including pygame 2.6.1.
- The clean copy collected and passed all 87 tests.
- Ruff lint passed.
- Ruff formatting check passed for all 39 Python files.
- The isolated audit directory and its generated virtual environment were removed after
  verification; the main workspace `.venv` was not modified by this audit.

This verifies the R0 clean-source bootstrap exit criterion. It is an engineering
verification result, not a pygame visual check or a new organism experiment.
