# RS-01 NEAT Feasibility and Lenia Architecture Verification

Date: 2026-07-26  
Runtime: CPython 3.13.13 on Windows  
Baseline: local and `origin/main` at `2bf55f6` (`Implement Bacterium-0 benchmark runner`)

## Scope and Boundaries

The execution gate began from a clean tracked worktree on local `main`, tracking
`origin/main`; R1B and its prior R1A baseline were committed. `NEXT_WORK_PACKET.md` remained
the R1C mainline packet. RS-01 added optional NEAT research code and Lenia/Godot documents
without changing environment, rewards, Q-learning, encoder, metrics, canonical benchmark
contract, historical results, or pygame implementation.

No Godot or pygame viewer was run. No human-visible result is claimed. No commit or push
was performed.

## Source Review

Official NEAT-Python documentation, PyPI metadata/release, the NEAT paper, OpenLenia
tutorial and paper, two MIT Godot reference repositories, and orientation sources were
reviewed. Exact source identities, licenses, access limitations, and the no-copy result are
recorded in `../research/lenia/SOURCE_PROVENANCE.md`.

The in-app browser was unavailable in this environment, so official pages were retrieved
read-only and repositories were cloned into a temporary directory for inspection. The
ConwayLife page returned HTTP 403. No external repository content was vendored.

## Commands and Results

### Optional dependency

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[dev,render,neat]"
```

Passed. Installed `neat-python==2.0.0` while retaining the editable project and existing
development/render dependencies.

### Configuration and dry run

```powershell
.\.venv\Scripts\python.exe -m abiogenesis.neuroevolution.trainer --dry-run
```

Passed. Resolved 13 ordered inputs, 5 ordered outputs, experiment seed 21, fitness roots
21-23, holdout roots 31-33, population 30, generations 10, five episodes per root, and one
worker. It reported that no run directory was created.

### Focused tests

An initial direct pytest invocation produced 12 passes and 3 setup errors because pytest
selected the known inaccessible machine temp root. This was not an application failure.
The project-authoritative test wrapper was then used:

```powershell
.\scripts\test.ps1 tests\test_neuroevolution_observation.py tests\test_neuroevolution_policy.py tests\test_neuroevolution_fitness.py tests\test_neuroevolution_integration.py -q
```

Passed: 15 tests. One warning came from NEAT-Python 2.0's exporter using deprecated
`datetime.utcnow()`.

### Full quality gate

```powershell
.\scripts\check.ps1
```

The first run passed all 126 tests and Ruff lint but failed the formatting check for one
new integration test file. After:

```powershell
.\.venv\Scripts\ruff.exe format tests\test_neuroevolution_integration.py
.\scripts\check.ps1
```

the complete gate passed: 126 tests, Ruff lint, and Ruff formatting. The same upstream
NEAT exporter warning remained.

### Bounded exploratory experiment

```powershell
.\.venv\Scripts\python.exe -m abiogenesis.neuroevolution.trainer
```

Passed in approximately 10 seconds and wrote ignored run
`runs/neat/20260726T152729Z_neat-feedforward-experimental_d927`. After the manifest gained
its explicit remaining-nondeterminism block, the same command was rerun and produced the
final verified artifact `runs/neat/20260726T153027Z_neat-feedforward-experimental_19fa`.
Both runs selected the same winner and produced the same reported fitness and holdout
metrics. The final artifact's status is `partial`
because the source tree necessarily contained uncommitted RS-01 changes. The only deviation
code is `source-not-clean`.

- Winner genome: 40
- Generation 0 maximum fitness: `0.940000000000002`
- Generation 9 maximum fitness: `1.19533333333333`
- Recomputed winner evolutionary fitness: `1.1953333333333345`
- Holdout mean reward: `-0.686`
- Exploratory random holdout mean: `-0.067333333333333`
- Exploratory local-Q holdout mean: `0.758`
- Exploratory novelty-scent-Q holdout mean: `-0.0673333333333334`

This is mixed/negative generalization evidence. It does not promote NEAT and does not alter
legacy benchmark findings.

### Artifact validation and ASCII replay

```powershell
.\.venv\Scripts\python.exe -m abiogenesis.neuroevolution.trainer --validate-only runs\neat\20260726T153027Z_neat-feedforward-experimental_19fa
.\.venv\Scripts\python.exe -m abiogenesis.neuroevolution.replay --neat-run runs\neat\20260726T153027Z_neat-feedforward-experimental_19fa --seed 31 --debug-overlay --max-frames 1
```

Both passed. Validation confirmed terminal partial state, required inventory, effective
configuration, seed separation, feature/action ordering, JSON parsing, and SHA-256 hashes.
Replay loaded the trusted local winner and executed one west action while printing the
input vectors, output scores, action, run ID, and genome ID.

### Final repository audit

```powershell
git diff --check
git diff --exit-code -- NEXT_WORK_PACKET.md docs\experiments.md docs\lab-notes.md docs\project-context.md
git status --short
git check-ignore -v runs\neat\20260726T153027Z_neat-feedforward-experimental_19fa\manifest.json
```

Passed. No whitespace errors were reported; the R1C packet and three historical evidence
files had no diff. Git reported only the intended source, tests, configuration, research
documentation, and control-plane changes. The retained run resolves to the `.gitignore`
`runs/` rule. Local `HEAD` and `origin/main` still resolve to
`2bf55f687990e0312d684e97624c426af3832e7e`.

## Verification Boundary

Automated results prove the optional dependency, Python adapters, evolution plumbing,
artifact lifecycle, hash validation, reload, and ASCII execution in this environment.
They do not prove cross-platform bit-for-bit evolution, canonical benchmark superiority,
pygame visual behavior, Godot compatibility, Lenia mathematics, or CPU/GPU parity.
