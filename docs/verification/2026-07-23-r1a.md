# R1A Verification Record — 2026-07-23

## Scope and Boundary

R1A specified Bacterium-0 benchmark v1 and finalized artifact contract `1.0`. It changed
documentation and synthetic examples only. It did not implement a runner, modify Python
runtime modules, alter historical experiment results, run the full suite, or run pygame.

## Evidence Review

The specification was reconciled against:

- `BacteriaWorldConfig` defaults;
- current Q-learning hyperparameter defaults and encoders;
- the evaluator's `seed + 100000` evaluation-root derivation;
- current `RunSummary` aggregation semantics;
- environment support for explicit food and poison counts;
- the documented and tested novelty reward value `0.02`;
- the existing R0 quick wrapper and Phase 3D historical record.

This review found that the current CLI cannot vary food/poison counts, use separate
training/evaluation configurations, select exactly the five canonical policies, or write
contract artifacts. Those are explicit R1B implementation requirements.

## Documentation Example Validation

Commands:

```powershell
.\.venv\Scripts\python.exe -m json.tool docs/experiments/examples/b0-quick-v1/manifest.json
.\.venv\Scripts\python.exe -m json.tool docs/experiments/examples/b0-quick-v1/metrics.json
```

Result: **passed**. Both files parsed as JSON.

PowerShell `Get-FileHash -Algorithm SHA256` verified:

- `metrics.json`:
  `382f473420bc77ca02109b8e666de11010e29e4025de0c7edebc543c3f702540`;
- `summary.md`:
  `1b9b13f9aa559574d6452ad13255eba59004cf0407ea29ccc3b337ce457c1b05`.

An inline Python standard-library audit verified all five policy records, the complete
15-metric key set, three ordered per-seed records per policy, equality of `sample_count`
and `episode_count`, and every aggregate `count`, mean, population standard deviation,
minimum, and maximum. Result: **passed**.

The examples are explicitly synthetic and cannot support behavior claims.

## Quality Gate

Command:

```powershell
.\scripts\check.ps1
```

Result: **passed** on Python 3.13.13.

- pytest: 87 passed in 2.55 seconds;
- Ruff lint: passed;
- Ruff format check: 39 files already formatted.

## Legacy Benchmark Regression

Command:

```powershell
.\scripts\run-benchmark.ps1
```

Result: **passed** in 11.7 seconds.

- replicate roots: 21-23;
- training episodes per Q agent: 200;
- evaluation episodes per seed: 20;
- grid: 10x10.

The output reproduced the historical Phase 3D table, including local-Q average reward
`1.704`, random average reward `0.227`, and novelty-scent average reward `0.137`. This was a
regression check, not a new canonical artifact run.

## Diff and Git Checks

`git diff --check` passed before and after the main documentation work. Final Git status and
diff statistics are reported in the R1A handoff; no commit or push was authorized or
performed.

## Skipped Verification

- `b0-full-v1` was not run; R1A explicitly prohibits the full ten-seed evidence run.
- Named scenario execution was not run because the R1B runner does not exist.
- Pygame was not run and no visual success is claimed.
- JSON Schema files/tests were not added. The written contract assigns cross-file,
  arithmetic, registry, and hash validation to a dependency-free R1B validator.

## Behavior-change Statement

No environment behavior, reward/default value, agent learning logic, encoder semantics,
renderer behavior, or recorded experiment result was changed.
